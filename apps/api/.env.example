# BVCP API 環境変数

# PostgreSQL 接続 (Docker Compose)
DATABASE_URL="postgresql://bvcp:bvcp_dev_password@localhost:5432/bvcp"

# JWT シークレット（本番環境では必ず変更）
JWT_SECRET="bvcp-dev-secret-change-in-production"

# サーバーポート
PORT=3001
