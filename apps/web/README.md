# apps/web

Next.js Web アプリケーション（未初期化）

## セットアップ

```bash
cd apps/web
npx create-next-app@latest . --typescript --app --eslint --tailwind --src-dir --import-alias "@/*"
```

## 開発

```bash
npm run dev
```
